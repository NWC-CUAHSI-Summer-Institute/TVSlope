# Text files stay as normal, diffable git objects.
*.ipynb text
*.md    text
*.py    text
*.csv   text
# Large source datasets (SWORD, FIMBench, staged HAND) are NOT committed -- fetch them (see README, get_data.py).
